{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "target": "ES2022",                      // 최신 문법 사용을 위해
    "module": "esnext",                      // ESM 모듈 시스템
    "moduleResolution": "node16",            // Tailwind v4의 .d.mts 타입 해결용 ★
    "jsx": "react-jsx",                      // React 17+ JSX 설정
    "strict": true,                          // 타입 안정성 (선택적이지만 권장)
    "esModuleInterop": true,                 // CommonJS 호환 (Vite 일부 패키지 필요)
    "skipLibCheck": true                     // 타입 검사 속도 향상 (선택)
  }
}
